#include<bits/stdc++.h>
using namespace std;

void traverse(string mid, string post, int l_mid, int r_mid, int l_post, int r_post) {

}; 

int main(void){
    string mid = ""; cin >> mid;
    string post = ""; cin >> post;
    /////your code
    int l_mid = 0, r_mid = mid.size() - 1, l_post = 0, r_post = mid.size() - 1; 
    traverse(mid, post, l_mid, r_mid, l_post, r_post);
    ////////
    return 0;
}
